// import {Game} from "./bowling";
import {expect} from 'chai';

describe('Bowling Game', () => {
    // let game: Game;
    //
    // beforeEach(() => {
    //     game = new Game();
    // });
    //
    // it('should return 1 for a single skittle knocked over', () => {
    //     game.roll(1);
    //     expect(game.score).to.equal(1); //check that the score = 1
    // });
});
